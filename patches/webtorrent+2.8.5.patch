diff --git a/node_modules/webtorrent/lib/torrent.js b/node_modules/webtorrent/lib/torrent.js
index 6ff0780..732abbe 100644
--- a/node_modules/webtorrent/lib/torrent.js
+++ b/node_modules/webtorrent/lib/torrent.js
@@ -278,7 +278,7 @@ export default class Torrent extends EventEmitter {
     if (parsedTorrent) {
       // Attempt to set infoHash property synchronously
       this.infoHash = parsedTorrent.infoHash
-      this._debugId = arr2hex(parsedTorrent.infoHash).substring(0, 7)
+      this._debugId = (typeof parsedTorrent.infoHash === 'string' ? parsedTorrent.infoHash : arr2hex(parsedTorrent.infoHash)).substring(0, 7)
       queueMicrotask(() => {
         if (this.destroyed) return
         this._onParsedTorrent(parsedTorrent)
@@ -327,7 +327,7 @@ export default class Torrent extends EventEmitter {
   }

   _processParsedTorrent (parsedTorrent) {
-    this._debugId = arr2hex(parsedTorrent.infoHash).substring(0, 7)
+    this._debugId = (typeof parsedTorrent.infoHash === 'string' ? parsedTorrent.infoHash : arr2hex(parsedTorrent.infoHash)).substring(0, 7)

     if (typeof this.private !== 'undefined') {
       // `private` option overrides default, only if it's defined
